package ${entityPackage};

#foreach($item in $entityImportNonJavas)
import ${item};
#if(!$foreach.hasNext)

#end
#end
#foreach($item in $entityImportJavas)
import ${item};
#if(!$foreach.hasNext)

#end
#end
/**
#if($tableRemarks)
 * ${tableRemarks}
 *
#end
 * generated by SnailGenerator
 *
 * @author Leon Song
 */
@EqualsAndHashCode(callSuper = true)
@Data
public class ${entityUpperClassName} extends BaseEntity {

#foreach($column in $selfColumns)
#if(${column.remark})
    /**
     * ${column.remark}
     */
#end
    private ${column.javaSimpleName} ${column.lowerCamelName};

#end
}
